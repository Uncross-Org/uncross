{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "install": {
          "cmds": [
            "yarn install --frozen-lockfile --production"
          ]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "node scripts/keeper.mjs --cluster devnet --cadence 3000 --freeze 300 --interval 20",
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1,
    "sleepApplication": false
  }
}
