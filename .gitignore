.claude/
.DS_Store

# Brand working files: the renderer's safe-area check overlay is a diagnostic,
# not a deliverable.
brand/kit/x/header-safe-areas.png

# Raw recorder frames: hundreds of JPEGs, regenerable, not deliverables.
brand/video/raw/
# Clip render intermediates: 210 PNG frames and the data-injected page.
brand/clip/frames/
brand/clip/.clip.rendered.html
brand/invite/frames/
brand/invite/.invite.rendered.html
brand/results/frames/
brand/results/.results.rendered.html
# Entry-path harness evidence: screenshots and the last fresh wallet, per run.
uncross/scripts/entry-*.png
uncross/scripts/entry-wallet.txt
